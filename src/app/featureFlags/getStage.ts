import { ALPHA, BETA, GAMMA, PROD } from "./constants";

/**
 * re-write this function to acurately determine current stage.
 * @returns string - name of current stage
 */
export default function getStage(): string {
  // for demo purposes, the stage is hard-coded
  return PROD;
}
